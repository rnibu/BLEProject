import 'package:flutter/material.dart';



class EditTextMessage extends StatelessWidget {
  const EditTextMessage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
